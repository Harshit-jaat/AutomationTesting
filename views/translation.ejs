<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Find Translations</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    input, button { padding: 8px; margin: 5px; }
    table { width: 100%; margin-top: 20px; border-collapse: collapse; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    pre { background: #f4f4f4; padding: 10px; margin-top: 20px; border: 1px solid #ddd; }
  </style>
</head>
<body>

  <h1>Translation Finder</h1>

  <form method="GET" action="/translate">
    <input type="text" name="text" placeholder="Enter English Text..." value="<%= query || '' %>" required />
    <button type="submit">Find Translations</button>
  </form>

  <% if (translations && translations.length > 0) { %>
    <% translations.forEach((item, index) => { %>
      <h2>Result <%= index + 1 %>: <%= item.key %></h2>

      <table>
        <thead>
          <tr>
            <th>Language</th>
            <th>Translation</th>
          </tr>
        </thead>
        <tbody>
          <% for (const lang in item.translations) { %>
            <tr>
              <td><%= lang %></td>
              <td><%= item.translations[lang] || '❌ Not Found' %></td>
            </tr>
          <% } %>
        </tbody>
      </table>

      <h3>JSON Format:</h3>
      <pre><%= JSON.stringify(item.translations, null, 2) %></pre>

    <% }); %>
  <% } else if (query) { %>
    <h2>No matching translations found for "<%= query %>"</h2>
  <% } %>

</body>
</html>
